
  function addNums(){
    var var1 = document.getElementById('var1').value;
    var var2 = document.getElementById('var2').value;
    document.getElementById('result').innerHTML = (parseInt(var1) + parseInt (var2));
  }

//Challenge 1: Create a script with 2 variables, each assigned to a number. Add them together and output the result to the console. Now do the same with two strings.

var x=2
var y=4
console.log (x + y);
